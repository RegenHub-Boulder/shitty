{
  "name": "shitty",
  "module": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/client/main.tsx --outdir dist --target browser --splitting --minify",
    "start": "NODE_ENV=production bun run src/server.ts",
    "db:migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.2"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0"
  }
}